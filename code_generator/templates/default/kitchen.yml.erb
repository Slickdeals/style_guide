---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-14.04
  - name: centos-6.6
  - name: centos-5.11

suites:
  - name: default
    run_list:
      - recipe[<%= cookbook_name %>::default]
    attributes:
  - name: chef11
    driver:
      require_chef_omnibus: 11.18.12
    run_list:
      - recipe[<%= cookbook_name %>::default]
    attributes:
